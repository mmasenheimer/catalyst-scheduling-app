{
  "name": "catalyst-backend",
  "version": "0.0.0",
  "private": true,
  "description": "CATalyst Studios backend — Slack integration and API layer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@slack/bolt": "^4.0.0",
    "dotenv": "^16.0.0"
  }
}
